Authority: on draft PR #16 at exact head `6e0bf793c982c251a6c489a65d5edc57c11ce7f4`, refresh only the PR body so it matches current reviewed evidence.

Keep the reviewed source SHAs and draft/experimental hold. Update integrated behavior to describe one latest eligible process per visible session, stable session selectors, a 24-hour inactive default, native model/provider/token/context facts with pressure, null unknown account/quota/cost, and killed as terminal. Update checks to 217 full-suite tests, 7 focused latest-process regression tests, package smoke/Ruff/secret checks, live audit of 12 out-of-order lists with 0 selection mismatches, and exact-head CI run `32211712388` with 6 of 6 checks passed. Update the cdesktop boundary to state that source-side 0.2.6 durable recovery proof passed, but no 0.2.6 prerelease exists because the R2 secret inventory is empty; a fresh `version_type=none` prerelease is required after explicit infrastructure approval. Preserve the multi-week operational proof gate.

Use `~/.local/bin/gh-axi`. Do not edit source, push, mark ready, merge, publish, dispatch workflows, or set secrets. Stop after verifying the PR remains draft at the supplied exact head and report completion.
