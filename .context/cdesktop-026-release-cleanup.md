Authority: after the currently running `prepare-db:check` reaches a terminal result, clean only the artifacts introduced by your own verification commands in this isolated worktree. Restore every tracked file to exact base `62cbae3dd0a7f2ea9783d9352ba87b1da252e5e2`, including formatter changes and generated `shared/types.ts`, and remove the transient untracked `crates/db/prepare_db.sqlite`. Do not preserve or commit these unrelated mechanical changes. Preserve only `.context/cdesktop-026-release-readiness.md` as the local report. Then verify `git diff --exit-code 62cbae3dd0a7f2ea9783d9352ba87b1da252e5e2 -- . ':(exclude).context'`, report the database-check outcome, and finish the release-readiness audit. Do not rerun broad formatting or checks, publish, tag, merge, or create a PR.
