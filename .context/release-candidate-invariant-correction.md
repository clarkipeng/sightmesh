Pre-commit invariant correction from live native data: `executor_action.selected_provider_id` is a cdesktop provider identifier, not a SightMesh subscription account. Use it only to populate provider through the native provider map; keep `account_id` null unless a native execution record directly identifies a pool account.

Also, `overview` should behave as the built-in agent deck, not a turn ledger. Select the latest non-dropped, non-devserver process per visible session before snapshot reads and projection. Then apply the 24-hour inactive bound while always retaining the latest active process. This prevents repeated normalized-snapshot calls for every message turn and gives one stable current card per agent. Preserve `--since` for recent completed agent cards. Add tests proving multiple processes in one session produce one card and only that process is snapshotted.
