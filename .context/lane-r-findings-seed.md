# Operator-observed candidate findings (seed for Lane R / follow-ups)

- 2026-08-20, 4 concurrent terra-high workers: sightmesh CLI reads (list, peers, policy resolution) time out while the daemon serves the workers. A policy WRITE requiring live @name resolution fails when the fleet is busy - the exact moment a manager most wants to set policies. Candidates: CLI timeout too aggressive, read path serialized behind heavy session traffic, or policy set should resolve names from local records without a live daemon round-trip. Doctrine 3/7 relevance: control-plane operations should not degrade with worker load.
- Signal policies applied so far: lane-q, lane-r (terminal + context-pressure:0.75). lane-o, lane-p pending retry.
- 2026-08-20, runtime 0.2.6 pinned, 4-worker load: steer to @lane-q was NON-ATOMIC - the interrupt killed the active turn, then the replace follow-up POST returned HTTP 500, leaving the session killed with no queued resume. Recovery required a manual durable message (auto-resume). Confirmed finding: steer must enqueue the follow-up durably BEFORE interrupting, or roll back the interrupt on enqueue failure. Same atomicity family as Lane J/K spawn evidence.
