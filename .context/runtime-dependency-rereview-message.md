Authority: independent rereview of the cdesktop blocker fix. Review exact clean pushed cdesktop head `398668b54ff5f725575f660cc0bca62a240996af` on draft PR #4 against prior reviewed `3d9ef0c7` and base `62cbae3d`. Required: prove `extractAndRun` always uses the archive provider even with a corrupt preexisting cached ZIP; run the launcher regression and release-contract/type checks; verify the manifest integrity chain remains intact; verify release-distribution checks are actually selected by PR CI for npx-cli and release-workflow changes; verify root LICENSE is copied before npm pack and the contract test cannot pass on missing markers. Correct the earlier report's stale PR number and check state, then update `.context/runtime-dependency-independent-review.md` with exact findings and proceed decision. SightMesh remains exact `4fec36b` with 10 of 10 checks. Exclusions: no source edit, push, PR comment, release, dispatch, ready transition, or merge. Stop after factual rereview.
