One small visibility requirement while this code is still uncommitted: include `context.pressure` when both native token total and context window are numeric and the window is nonzero, using the same safe calculation already present in `cmd_peek`. The public fleet schema already permits `pressure`. Add the focused assertion. This makes the context field operationally useful without inferring anything.
