Queued exact workflow syntax check before final checkpoint: the new output key contains a hyphen. In GitHub expressions, use bracket access for it, for example `needs.changes.outputs['release-distribution']`, or rename the key to `release_distribution`. Do not leave `needs.changes.outputs.release-distribution`, which can parse as subtraction or invalid property access. Preserve the current blocker-fix scope and rerun YAML and action validation.
