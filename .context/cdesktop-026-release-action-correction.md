Immediate report correction before completion: there is no GitHub prerelease for `v0.2.6-20260816210919`; run `31972553761` skipped create-prerelease after R2 upload failed. Do not say to convert an existing prerelease or reuse its expired artifacts.

The truthful smallest operator path, after explicit approval and configuring all required repository secrets, is to dispatch `.github/workflows/pre-release.yml` from current `main` with `version_type=none`. That reuses package version 0.2.6, mints a fresh timestamped tag, rebuilds all artifacts, uploads them, and creates a new GitHub prerelease. Required secret names from the workflow are `R2_BINARIES_ACCESS_KEY_ID`, `R2_BINARIES_SECRET_ACCESS_KEY`, `R2_BINARIES_ENDPOINT`, `R2_BINARIES_BUCKET`, and `R2_BINARIES_PUBLIC_URL`; current repository secret inventory is empty. SightMesh must then pin the newly created tag/tarball. Promoting that prerelease to a full release is a separate explicit decision because it triggers npm publish.

Update only `.context/cdesktop-026-release-readiness.md` with this correction. Keep source exactly at `62cbae3dd0a7f2ea9783d9352ba87b1da252e5e2`, verify the report and clean source tree, and complete. Do not publish, dispatch, set secrets, commit, or run more checks.
