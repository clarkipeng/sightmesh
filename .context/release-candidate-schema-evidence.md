Additional native-schema evidence from the live spawn response: execution configuration is nested under `executor_action.typ.executor_config` with `executor`, optional `model_id`, and `reasoning_id`; the selected provider is under `executor_action.typ.config.selected_provider_id`. Existing `cmd_peek` already extracts authoritative `token_usage_info.total_tokens` and `model_context_window` from `normalized_snapshot` and computes context pressure. Please reuse that small existing parser or abstraction after bounding the selected processes, rather than reading the nonexistent top-level process fields or inventing new facts. Map provider identifiers only through the native provider inventory and keep account identity unknown unless cdesktop directly reports it.
