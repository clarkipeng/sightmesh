Exact source pointers so you can proceed without interrupting the integration owner: SightMesh PR #16 recovery is in `src/sightmesh/durable.py` with focused coverage in `tests/test_durable.py`. cdesktop exact `origin/main` implements feature inputs in `crates/server/src/routes/sessions/mod.rs` around `defer_dispatch`, `GET /sessions/:id/commands`, `POST /sessions/:id/commands/requeue`, and `POST /sessions/:id/commands/dispatch`; persistence and duplicate-safety tests are in `crates/db/src/models/session_command.rs`, especially `requeue_execution`, `requeue_killed_execution`, and tests for terminal requeue, duplicate reopen, and original dedupe keys. Please run the narrow database and route tests now and produce the disposable proof report. Do not contact or steer the integration owner again unless a concrete defect requires it.
