One native-state correction from the original live output: cdesktop reports `killed` as a normal terminal execution state after steer, replace, or operator stop. `fleet.TERMINAL` currently omits it, which misclassifies recent killed cards as unknown Needs attention. Add `killed` to the terminal set and focused decision/grouping coverage. Keep `failed`, `blocked`, and `stalled` in Needs attention through the existing higher-priority decision branch.
